# Colab already ships core scientific/GPU packages such as numpy, pillow,
# opencv-python, scipy, matplotlib, torch, torchvision, and tqdm. Installing
# or pinning them here can replace loaded Colab packages and repeatedly prompt
# for a runtime restart. Keep this file to missing runtime packages.
imageai==3.0.3
ffmpeg-python==0.2.0
gdown
addict
yapf
lmdb
av
